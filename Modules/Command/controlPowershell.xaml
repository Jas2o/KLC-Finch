<UserControl x:Class="KLC_Finch.controlPowershell"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <StackPanel Orientation="Horizontal">
            <Button x:Name="btnCommandStart" Content="Start" Width="75" Click="btnCommandStart_Click" Margin="0,0,5,0"/>
            <Button x:Name="btnCommandKill" Content="KillCommand" Width="85" Margin="0,0,5,0" Click="btnCommandKill_Click"/>
            <!--<Button x:Name="btnCommandClear" Content="Clear" Width="75" Margin="0,0,5,0" Click="btnCommandClear_Click" />-->
            <Label Content="Scrollback:" Margin="0,0,5,0" VerticalAlignment="Center" />
            <Button x:Name="btnCommandScrollback" Content="Little (Fast)" Width="75" Margin="0,0,5,0" Click="btnCommandScrollback_Click" />
            <Button x:Name="btnCommandScrollbackSlow" Content="Far (Slow)" Width="75" Margin="0,0,5,0" Click="btnCommandScrollbackSlow_Click" />
            <CheckBox x:Name="chkAllowColour" Content="Allow Colour" VerticalAlignment="Center" />
        </StackPanel>
        
        <TextBox x:Name="txtCommand" Grid.Row="1" Margin="0,5,0,0" Background="MidnightBlue" Foreground="White" FontFamily="Consolas" IsReadOnly="true" VerticalScrollBarVisibility="Auto" />
        <RichTextBox x:Name="richCommand" Grid.Row="1" Margin="0,5,0,0" Background="MidnightBlue" Foreground="White" FontFamily="Consolas" IsReadOnly="true" VerticalScrollBarVisibility="Auto">
            <RichTextBox.Resources>
                <Style TargetType="{x:Type Paragraph}">
                    <Setter Property="Margin" Value="0"/>
                </Style>
            </RichTextBox.Resources>
        </RichTextBox>
        
        <Grid Grid.Row="2" Margin="0,5,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="txtCommandInput" TextWrapping="Wrap" AcceptsReturn="true" VerticalScrollBarVisibility="Auto"  PreviewKeyDown="txtCommandInput_PreviewKeyDown"/>
            <Button x:Name="btnCommandLineMode" Content="Line Mode" HorizontalAlignment="Left" VerticalAlignment="Top" Width="75" Grid.Column="1" Margin="5,0,0,0" Click="btnCommandLineMode_Click" IsEnabled="False" />
        </Grid>
    </Grid>
</UserControl>
